// Code JavaScript ici (si nécessaire)

// Exemple : Ajoutez une interaction simple
document.addEventListener("DOMContentLoaded", function () {
    // Sélectionnez un élément
    const myElement = document.querySelector("#accueil");

    // Ajoutez un événement de clic
    myElement.addEventListener("click", function () {
        alert("Vous avez cliqué sur la section d'accueil !");
    });
    
    // Vous pouvez ajouter plus de fonctionnalités ici en fonction de vos besoins.
});
